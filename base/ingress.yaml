apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: "nginx"
  name: ${SERVICE_NAME}-ingress
spec:
  rules:
  - host: ${SERVICE_NAME}.dev2.appetize-dev.com
    http:
      paths:
      - backend:
          serviceName: ${SERVICE_NAME}
          servicePort: 80